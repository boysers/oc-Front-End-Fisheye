"use strict";

/**
 * Checks if the value is an instance of HTMLElement.
 * @param {HTMLElement} element
 * @returns {boolean}
 */
export function isInstanceofHTMLElement(element) {
	const isHTMLElement = element instanceof HTMLElement;
	return isHTMLElement;
}
